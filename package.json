{
  "name": "eon-study",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:unit": "node --test tests/unit/*.test.js",
    "test:e2e": "playwright test tests/e2e --workers=1",
    "check": "node --check assets/js/app.js && node --check assets/js/storage.js && node --check assets/js/cloud-sync.js && node --check functions/api/_platform.js"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
